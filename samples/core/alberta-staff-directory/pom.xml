<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>org.icefaces.samples</groupId>
    <artifactId>alberta-staff-directory</artifactId>
    <version>3.3.0</version>
    <packaging>war</packaging>
    <name>Alberta Staff Directory</name>
    <description>ICEfaces clone of Alberta government staff directory</description>

    <properties>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <!-- ICEfaces Core - Local JAR -->
        <dependency>
            <groupId>org.icefaces</groupId>
            <artifactId>icefaces</artifactId>
            <version>3.3.0</version>
            <scope>system</scope>
            <systemPath>${basedir}/../../../lib/icefaces.jar</systemPath>
        </dependency>

        <!-- JSF Implementation - Mojarra -->
        <dependency>
            <groupId>com.sun.faces</groupId>
            <artifactId>jsf-api</artifactId>
            <version>2.1.26</version>
            <scope>system</scope>
            <systemPath>${basedir}/../../../lib/mojarra/2.1/javax.faces-2.1.26.jar</systemPath>
        </dependency>

        <!-- Servlet API -->
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>servlet-api</artifactId>
            <version>2.5</version>
            <scope>provided</scope>
        </dependency>

        <!-- JSP API -->
        <dependency>
            <groupId>javax.servlet.jsp</groupId>
            <artifactId>jsp-api</artifactId>
            <version>2.1</version>
            <scope>provided</scope>
        </dependency>

        <!-- EL API -->
        <dependency>
            <groupId>javax.el</groupId>
            <artifactId>el-api</artifactId>
            <version>2.2.1-b03</version>
            <scope>system</scope>
            <systemPath>${basedir}/../../../lib/el-api-2.2.1-b03.jar</systemPath>
        </dependency>

        <!-- Annotation API -->
        <dependency>
            <groupId>javax.annotation</groupId>
            <artifactId>jsr250-api</artifactId>
            <version>1.0</version>
            <scope>system</scope>
            <systemPath>${basedir}/../../../lib/javax.annotation.jar</systemPath>
        </dependency>

        <!-- MySQL Connector for database access -->
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>8.0.33</version>
        </dependency>

    </dependencies>

    <build>
        <finalName>alberta-staff-directory</finalName>
        
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.2.3</version>
                <configuration>
                    <warName>alberta-staff-directory</warName>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                    <webResources>
                        <resource>
                            <directory>${basedir}/../../../lib</directory>
                            <targetPath>WEB-INF/lib</targetPath>
                            <includes>
                                <include>icefaces.jar</include>
                                <include>el-api-2.2.1-b03.jar</include>
                                <include>javax.annotation.jar</include>
                            </includes>
                        </resource>
                        <resource>
                            <directory>${basedir}/../../../lib/mojarra/2.1</directory>
                            <targetPath>WEB-INF/lib</targetPath>
                            <includes>
                                <include>javax.faces-2.1.26.jar</include>
                            </includes>
                        </resource>
                    </webResources>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>